161-vid Feb 11 2026

Interface
    Interface --> abstract type --> single specification for implement functionality
    enum implements Interface
    record implements Interface
    class implements Interface
    Interface extends "Another" Interface
    abstract implements Interface

    Summary: An abstract class provides common definition, as a base class, that multiple
    derived classes can share

        when to use?
    - share code among serveral closely related classes
    - expect classes to extends your abstract class to have common methods, fields, or access

    Summary: the Interface decouples the "what" from the "how", and is used
    to make different types behave in similar ways

        when to use?
    - you expect that unrelated classes will implement your interface
    - you want to specify the behavior of a particular data type, but not concerned 
    about who implements its behavior

Generic
    Generic class 
        class Name<>{} --> using <T> any type allowed
        can include many types class Name<T, S, U, V...>{}
        bound class Name<T extends Interface/Class/Interface + Class/Multiple Interfaces> 

172 Feb 12 2026

Comparator --> interface use to make object comparable
    int compare(T o1, T o2) --> compare two same type with each other

Comparable --> interface use to make object comparable
    int comapreTo(T o) --> compare the arguments with the current instance

Generic methods
    public <T> String method(T input){}
    public <T extends ....> method(T input){} --> adding bound
    List<? extends ....> input --> ? is a wild card for type
    List<? super ...> input --> ? super here is lower bound

178 - nested classes, local types & annonymous classes

    Encapsulation and access rules
        private or public or protect
    local classes
        final 
    annonymous class --> local with no Name , nested inside other class

186 - Lambda Expressions

    similar to what we been using in javascript
    a functional interface is an interface that has one and only one, abstract method

    Lambda Expressions: is a funcitonal programming approach (modern way)
        just compute and return result (quick and easy)
    
    Consumer - Function - Predicate - Supplier
        consumer: void accept(T t) --> execute code without returning data
        function: R apply(T t) --> return result of an operation
        predicate: boolean test(T t) --> test if true or false
        supplier: T get() --> return an instance of something

207 - Hash

    Hash Collection 
        HashSet
        LinkedHashSet
        HashMap
        LinkedHashMap
    Set (unique, no duplicate)
        derive from object  
        access to equals or hashcode method override
        operation: union, intersection, difference

217 Feb 15 2026

    Map another data structure in collection framework
        key value pairs
    EnumSet: set implementation for use with enum values
    EnumMap: map implementation for use with enum type keys

228 Feb 16 2026

    Mutable: not remains constant
    Immutable: remains constant (has some side effects)
    Modifier:
        Final --> just like the word "FINAL"
            public final
            protected final
            private final 
            --overriding vs hiding
    Shallow copied: copy surface or structure not elements
    Deep copied: copy surface and strucutre and elements
    
240 Feb 17 2026

    Compact Constructor: is declared with no parentheses so no arguments
    Enum Constructor: 
    Static Initializer

247 Feb 18 2026

    Sealed new Modifier for classes and Interfaces
    Stream a sequence of elements supporting sequential and parallel aggregate operations
        the stream and collection types were designed for different purposes
        a collection is used to store and manage a series of elements in java providing direct access to
            the collection elements
    list.stream().limit().filter().map().sort().foreach() etc....
        it just linked the method together like this chain methods

257 Feb 19 2026

    stream() --> as the word suggest it is a stream
        and it has a lot of methods to manipulate the stream of inputs
        and producing the outputs
    terminal operations: count(), max(), min(), average(), sum(), summaryStatistics()
    boolean stream method will take Predicate function to deciding whether to return false or true
    
254 Feb 21 2026

    more stream method and linking
    distinct(), filter()-takeWhile()-dropWhile(), limit(), map(), peek(), skip(), sorted()-sorted(Comparator)
    count(), max(), min(), average(), sum(), summaryStatistics()
    allMatch(), anyMatch(), noneMatch()

    collect(), reduce(), toArray(), toList(), averageDouble()
    averageInt(), averageLong(), counting(), collectingAndThen()
    filtering()
    isPresent(), orElse()

    static import: import static java.util.stream.Collectors.*
    let you import one or more static members of a class
    flatMap --> flat to one map

260 Feb 22 2026

    overflow,underflow
    java.util.Math
    Math.random; seed or no seed, range in between
    unique seed System.nanoTime(); --> current system nanoTime
    
274 Feb 23 2026

    BigDecimal in dept
        precision representation is difficult for number xxxx.xxxxx
        scale and unscale value

    java.time
        class with time information
    LocalDate
    Date
    Calendar
    LocalTime
    -temporalField: represents a specific field within
        a date-time, such as MONTH_OF_YEAR, DAY_OF_WEEK,
        HOUR_OF_DAY
    epoch time 
    System.currentTimeMillis
    System.nanoTime
    Localization--> different location display different format of time 
        localization help with that depends on where the application is run at that time
    Locale --> locality of the places
    Internationalization --> designing to help with this concept of localization

    Formatting: Dates, Numbers, Currencies
        DateTimeFormatter help with format the date
        Or we could use free package to help with numerical values
    
    ResourceBundle for Multi-language supporting

    Regular Expression
        String mostly
        combinations characters has special meaning
    
    Literals: one to one match
    Character Classes: predefined or defined yourself 
        .[abc] [a-g] [A-Z] etc...
    Quantifiers: metacharacter identify to make a match
        * + ?
    Boundary Matcher: position in the text
        ^ $ \b
    Groups: identify for capturing subexpression
        ()

301 Feb 26 2026

    directory --> file system container for others directories
    path --> either a directory or filename
    root directory --> top level
    current directory --> current level
    absolute path --> includes everything
    relative path --> path relative to current
    
305 Feb 27 2026

    dept first --> visit all childs or any folder sibling up
    breath first --> dependent nodes or sibling nodes walk
    FileVisitor<> interface 
    
    Read file
    write file
    copy/move
    delete
    check info of file
    list dir
    walk tree
    watch changes

    InputStream 
        abstract class representing an input stream of bytes
    
    Encoding characters 
        to transfer character in computer
    
316 Mar 01 2026

    file and directory manipulation
    RandomAccessFile --> access file at any time 

324 Mar 02 2026

    Thread --> create, info, instantiation

329 Mar 07 2026

    Thread 
        interference -> another active thread has an open door
            to that same unit of work, where the paused thread is
            only partially done
    Interleaving -> threads run concurrently which overlap or interleave in time
    Atomic actions -> happens all at once
        action run until complete cannot interrupt
    volatile --> modifier --> indicator that variable may changes by multiple thread
        has limited usage
    
    Sychronization -> block critical section and let one thread run at a time
        Critical section: where shared resource happened
    
    Sychronization feature in Java allow you to lock certain part of section
        instead of blocking it entirely
    
333 Mar 8 2026

    synchronize(this.-object--field){
        //critical or share resource code here
    }

    Producer and Consumer concepts

    Mutex lock
        lock()
        unlock()
    explicit control-- either lock or release
    timeout-- without blocking indefinitely
    interrupt locking-- handling acquisition more gracefully
    improve debugging-- query of waiting locks, check if thread hold lock
    
    callable

344 Mar 11 2026

    hashmap -> lack thread synchronization

345 Mar 11 2026

    normal stream and parrallel stream
    concurrent collection
    arrayblockingqueue -- thread safe -- for shared resource logic
    deadlock: loop
    livelock: two or more loop
    starvation: unable to continue due to indefinitely waiting

    fair lock: ensure for thread to access resource faily and orderly fashion
    
    atomic variable: help with all above (class)
    watchservice: manitoring changes and allow to do something base on changes
    

362 March 13 2026

    SQL Injection: string to inject to database for leak data
        clean input before let it doing anything with database
    
    ANSI SQL: America National Standard Institute standard query language
    
     




